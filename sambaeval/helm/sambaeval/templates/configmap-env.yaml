apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "sambaeval.configMapName" . }}
  labels:
    {{- include "sambaeval.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.env }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
